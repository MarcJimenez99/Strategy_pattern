#ifndef _LIST_CONTAINER_HPP
#define _LISt_CONTAINER_HPP

#include "sort.hpp"
#include "base.hpp"

class Sort;
class Base;

class ListContainer {
	protected:
		Sort* sort_function;
		list<Container*>'
	public:
		void set_sort_function(Sort* sort_function) {
			//should point to an algorithm
		}
		void add_element(Base* element) {
		
		}
